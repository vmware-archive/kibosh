---
name: kibosh-example
icon_file: resources/icon.jpg
label: Kibosh Example Tile
description: Kibosh Example Tile

# When tile-generator has a "kibosh" type, use a tile.yml that looks like this.

packages:

- name: my-helm-app
  type: kibosh
  path: local/path/to/helm/chart
  plans:
  - name: "default"
    description: "default plan for spacebears"
    file: "default.yaml"

