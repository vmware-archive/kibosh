#!/bin/bash

set -e

export LOG_DIR=/var/vcap/sys/log/delete-all-and-deregister

mkdir -p "$LOG_DIR"

python3 /var/vcap/jobs/delete-all-and-deregister/bin/errand.py \
    > >(tee -a "$LOG_DIR/errand.stdout.log") \
    2> >(tee -a "$LOG_DIR/errand.stderr.log" >&2)
