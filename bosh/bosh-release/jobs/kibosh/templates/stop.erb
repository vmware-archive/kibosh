#!/usr/bin/env bash

# If a command fails, exit immediately
set -e

PIDFILE="/var/vcap/sys/run/kibosh/kibosh.pid"

/sbin/start-stop-daemon \
  --pidfile "$PIDFILE" \
  --retry TERM/20/QUIT/1/KILL \
  --oknodo \
  --stop

rm /var/vcap/sys/run/kibosh/kibosh.pid
